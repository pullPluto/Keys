# Application registry package

Provider-neutral contract for draft application registration. A delivery adapter must validate the protocol, exact redirect URI rules, caller permission, approval requirement, and audit write before it can persist or activate an application.
