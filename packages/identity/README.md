# Identity package

Organization-scoped identity contracts. Adapters that persist or resolve identities belong outside this package so the model stays independent of Cloudflare and any external identity provider.
